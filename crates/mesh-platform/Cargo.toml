[package]
name = "mesh-platform"
version = "0.0.0"
description = "Platform portability shims over libc for the mesh shell."
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
libc = "0.2.186"
